version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX   # we handle versioning at repo level, not package name
breaking:
  use:
    - WIRE_COMPATIBLE          # only flag wire-breaking changes, not source-breaking